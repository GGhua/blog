name: Hugo build and deploy
on:
  schedule:
    # 每天 UTC 00:00 重建站点
    - cron: '0 0 * * *'
  push:
    branches: [ main ]
    paths:
      - 'assets/**'
      - 'config/**'
      - 'content/**'
      - 'data/**'
      - 'static/**'
      - 'themes/**'
      - 'go.mod'
      - 'go.sum'
  workflow_run:
    workflows: ['Update theme']
    types:
      - completed
  workflow_dispatch: # 允许手动触发

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          ssh-key: ${{ secrets.SYNC_CONTENT }}
          # submodules: true # 获取 Hugo 主题
          submodules: false
          fetch-depth: 0 # 用于 .GitInfo 和 .Lastmod

      - name: Cache Hugo resources
        uses: actions/cache@v6
        env:
          cache-name: cache-hugo-resources
        with:
          path: resources
          key: ${{ env.cache-name }}
      # 手动修复并拉取最新子模块，绕过坏 commit
      - name: Fix & update FixIt theme
        run: |
          git config user.name "Cell[bot]"
          git config user.email "48508169+GGhua@users.noreply.github.com"
          git submodule sync
          git submodule update --init --remote --force themes/FixIt

      - name: Setup Hugo
        uses: peaceiris/actions-hugo@v3
        with:
          hugo-version: ${{ vars.HUGO_VERSION || 'latest' }}
          extended: true

      - name: Build site
        env:
          HUGO_PARAMS_GHTOKEN: ${{ secrets.HUGO_PARAMS_GHTOKEN }}
        run: hugo --gc --minify --logLevel info

      - name: Deploy to gghua.github.io
        uses: peaceiris/actions-gh-pages@v4
        with:
          deploy_key: ${{ secrets.GP_DEPLOY_KEY }} # 需在仓库 secrets 中配置部署密钥
          external_repository: GGHua/GGHua.github.io # 目标仓库
          publish_branch: main # 目标分支
          publish_dir: ./public # Hugo 构建产物目录
          user_name: 'Cell[bot]'
          user_email: '48508169+GGhua@users.noreply.github.com'
          commit_message: ':rocket:'
          # commit_message: ':rocket: Deploy from ${{ github.repository }} (${{ github.sha }})'
          force_orphan: false



